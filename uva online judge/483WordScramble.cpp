#include<bits/stdc++.h>
using namespace std;
int main()
{
    int i,j,k;
    char ch[10000],ch2[10000];
    long long int a,b,c,n,t;
    while(true)
    {
        //scanf("%s",&ch);       //space bihin length count korte chaile
        gets(ch);  //space soho length korte chaile
        n=strlen(ch);
       // cout<<n<<endl;
        for(i=n-1; i>=0; i--)
        {
            //printf("%s",ch[i]);
            //cout<<ch[i];
            if(ch[i]==' ')
            {
                //cout<<" ";
            }
        }

    }
    cout<<endl;
    return 0;
}
