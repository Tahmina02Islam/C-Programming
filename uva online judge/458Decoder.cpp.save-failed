#include<bits/stdc++.h>
using namespace std;
int main()
{
    int i,j,k;
    int t,n,a,b,c,d;
    char ar[100],str[100];
    while(gets(ar))
    {
        int len=strlen(ar);
        cout<<" Length : "<<len<<endl;
        for(i=0; i<len; i++)
        {
            str[i]=ar[i]-7;
            cout<<str[i];
        }
        cout<<endl;
    }


    return 0;
}
