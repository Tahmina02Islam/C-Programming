#include<bits/stdc++.h>
using namespace std;
int main()
{
   int i=0,j=0,k,count=0,p,a[100],b,c=0,len;
   //char ch[1000];
   //gets(ch);
   string str;
  //while(ch[i]!='\0')
  while(getline(cin,str))
   {
      /* if((ch[i]>='a' && ch[i]<='z') || (ch[i]>='A' && ch[i]<='Z'))
       {
           count++;
           p=count;
           if(ch[i]==' ')
           {
               count=0;
               a[j]=p;
               j++;
           }
       }*/
       len=str.size();
       cout<<len<<endl;
   }
}
